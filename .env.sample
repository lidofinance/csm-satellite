RPC_URL=
# Specify the chain to deploy or test on
CHAIN=mainnet | hoodi
# Admin address for the OssifiableProxy - required by `just deploy` (including local Anvil)
PROXY_ADMIN=
# Existing proxy to upgrade - required by `just upgrade-live` and `just upgrade-live-dry`
PROXY_ADDRESS=
